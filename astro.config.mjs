// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set to your production URL for correct canonical and Open Graph URLs.
  site: 'https://clock.krabhi4.in',
});
